/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'content.wepik.com', 'img.freepik.com','liveblocks.io'],
  },
}

export default nextConfig
